{% extends 'layout/layout.html.twig' %}

{% block content %}


<div class="container">
                    <form method="post" action="../public/index.php?id={{ article.id }}access=article!update">

                        <div class="form-group">
                            <label for="title"
                                   class="col-form-label">Titre:</label>
                            <input type="text" class="form-control" id="title" name="title" value="{{ article.title }}" >
                        </div>

                        <div class="form-group">
                            <label for="chapo"
                                   class="col-form-label">Châpo:</label>
                            <input type="text" name="chapo" id="chapo" class="form-control" value="{{ article.chapo }}">

                        </div>
                        <div class="form-group">
                            <label for="content"
                                   class="col-form-label">Article:</label>
                            <textarea class="form-control" id="content"
                                      name="content">{{ article.content }}</textarea>
                        </div>
                    </form>

                <div class="footer">
                    <button type="button" class="btn btn-secondary">
                        Annuler
                    </button>
                    <a class="btn btn-primary" type="submit"
                       href="../public/?id={{ article.id }}access=article!update">Valider</a>
                </div>
</div>
{% endblock %}
